P = load("data.txt");
D = P(:, 3:4); % D(1) = date_decimal, D(2) = value

% model F1(a, b, c, d, t) = a1*t + b1 + c1 * sin(2πt) + d1 cos(2πt)
for i = 1:size(D, 1)
    t = D(i, 1);
    A(i, :) = [t, 1, sin(2*pi*t), cos(2*pi*t)];
end


